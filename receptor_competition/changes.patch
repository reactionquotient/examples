--- a/receptor_competition/receptor_competition.py
+++ b/receptor_competition/receptor_competition.py
@@
-# Dose grids
-L_grid = np.logspace(-2, 3, 200)  # 0.01*Kd_L to 1000*Kd_L (relative to Kd_L)
-C_levels = [0.0, 0.3*Kd_C, 1.0*Kd_C, 3.0*Kd_C]  # no antag -> strong antag
-
-def frac_L_bound(L, C):
-    # Ligand excess approximation => simple closed form for competitive binding
-    return (Keq_L * L) / (1.0 + Keq_L * L + Keq_C * C)
+# Normalized grids (dimensionless)
+L_over_Kd = np.logspace(-2, 4, 200)    # [L]/Kd_L from 0.01 to 10,000
+C_over_Kd_levels = [0.0, 0.3, 1.0, 3.0]
+
+def frac_L_bound_norm(Lk, Ck):
+    # In normalized units, f = Lk / (1 + Lk + Ck)
+    return Lk / (1.0 + Lk + Ck)
@@
-plt.figure(figsize=(5,4))
-for C in C_levels:
-    f = frac_L_bound(L_grid, C)
-    plt.semilogx(L_grid, 100*f, label=f"[C]={C:.1f}")
-
-plt.xlabel("[L] (same units as Kd)")
+plt.figure(figsize=(6,4.5))
+for Ck in C_over_Kd_levels:
+    f = frac_L_bound_norm(L_over_Kd, Ck)
+    plt.semilogx(L_over_Kd, 100*f, label=f"C/Kd_C={Ck:g}")
+
+plt.xlabel("[L]/Kd_L (dimensionless)")
 plt.ylabel("% receptor bound by L")
-plt.title("Competitive binding: dose–response shifts with antagonist")
-plt.legend(frameon=False)
-plt.tight_layout()
-plt.savefig("receptor_competition_dose_response.png", dpi=200)
+plt.title("Competitive binding:\nDose–response shifts with antagonist (normalized units)")
+plt.legend(frameon=False)
+plt.tight_layout(pad=1.2)
+plt.savefig("receptor_competition_dose_response.png", dpi=200, bbox_inches="tight")

